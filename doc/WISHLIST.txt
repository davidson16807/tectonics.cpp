

HOT PATH TO PARITY:
insolation
climate
precipitation
erosion
lithification
metamorphosis
pbr surfaces
pbr atmospheres
continent congestion

NICE TO HAVES:
pbr volumetric clouds
pinn based climate
boid demo
consolidate FullTable/PartialTable paradigm with *Component paradigm
implement alternative to CommonComponent akin to FullTable
remove remaining instances of index/whole.hpp
remove remaining instances of index/known.hpp
see if Makefiles can be simplified using `uname -o`
drop suppressed warnings (-Wno-deprecated-declarations -Wno-braced-scalar-init -Wno-unused-variable -Wno-vexing-parse -Wno-macro-redefined) when building on mac and fix code that is generating them

check that macro switches (e.g. index/adapted/glm/GlmStrings.hpp) work outside of mac

MIGHT AS WELL:
add speed control to plates and interaction demos
move ECS from orrery to track?
CoordinateSystemBuffer, composes PyramidBuffers to form coordinate system indicator
read compound:: properties from tsv files using DelimitedTableLineCodec
property based tests for *Components
tiny:: for cell properties where every byte counts
huge:: for high precision over long distances using scalars wrapping int64 
2d analytic functions
FirstPersonNavigationUpdater/FirstPersonNavigationState
RocketNavigationUpdater/RocketNavigationState
OrbitBuffer class, has a storeLines() method that accepts an orbit and stores sample points along the orbit up to either the period or an amount of time that is passed in through the constructor
TextSwarmShaderProgram, letters are billboards that are constant-sized in screen coordinates and textured from image file of the alphabet
geometry library for all mensuration and intersection tasks in the style of `geometric::`
property based unit tests for all functions in the library above
analytic:: classes for multivariate functions
property based unit tests for all the above functions
property based tests and working functionality for tiny::
property based tests and working functionality for `VectorCalculusByCurvilinearMethod`
property based tests and working functionality for `VectorCalculusByWeightedMultilinearMethod`
parse 3d models from file

TEST LIBRARY FUNCTIONALITY:
covariance? (i.e. scaling laws)
idempotent composition and other properties on composition
left/right_antitransitivity
injectivity
Lipschitz continuity for a derivative bounds
equivariance ("commutative square")

