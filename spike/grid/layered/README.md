`layered::` contains operations that can be performed on any layered grid, where layered grids are grids 
for which each vertex has multiple cells that are offset in a direction that's specified by the grid.
